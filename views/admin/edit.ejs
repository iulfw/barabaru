<!DOCTYPE html>
<html>

<head>

    <title>BARABIRU &nbsp|&nbsp Dashboard</title>
    <link rel="icon" href="assets/favicon.png">

    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="//cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.css">

    <link rel="preconnect" href="https://fonts.googleapis.com">
	<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
	<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Baloo+2:wght@400..800&display=swap">

    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <style>

    * {
        font-family: "Baloo 2";
    }

    body {
        background: #fdfdfd;
    }

    .form-btn {
        padding-top: 20px;
    }

    .btn {
        font-weight: 600;
        padding: 5px 25px;
        border: none;
    }

    .btn-primary {
        background: #3068e1;
    }

    .btn-primary:hover {
        background: #427af6;
    }

    </style>

</head>

<body>

    <div class="container mt-5">
        <div class="row">
            <div class="col-md-12">
                <% if (messages.error) { %>
                    <div class="alert alert-danger" role="alert"><%- messages.error %></div>
                <% } %>
                <div class="card border-0 shadow rounded">
                    <div class="card-body">
                        <form action="/admin/update/<%= id %>" method="post" enctype="multipart/form-data">
                            <div class="form-group">
                                <label>ID</label>
                                <input type="text" class="form-control" name="id" value="<%= id %>">
                            </div>
                            <div class="form-group">
                                <label>Type</label>
                                <select class="form-control" name="type">
                                    <option value="basic" <%= type == 'basic'? 'selected' : '' %>>Basic</option>
                                    <option value="non-coffee" <%= type == 'non-coffee'? 'selected' : '' %>>Non-Coffee</option>
                                    <option value="coffee-mixed" <%= type == 'coffee-mixed'? 'selected' : '' %>>Coffee Mixed</option>
                                    <option value="flavour" <%= type == 'flavour'? 'selected' : '' %>>Flavour</option>
                                    <option value="frappe" <%= type == 'frappe'? 'selected' : '' %>>Frappe</option>
                                    <option value="main-dish" <%= type == 'main-dish'? 'selected' : '' %>>Main Dish</option>
                                    <option value="snack" <%= type == 'snack'? 'selected' : '' %>>Snack</option>
                                    <option value="other" <%= type == 'other'? 'selected' : '' %>>Other</option>
                                </select>
                            </div>
                            <div class="form-group">
                                <label>Pic</label>
                                <div style="margin: 10px 0 20px;"><img src="<%= pic %>" alt="<%= pic %>" style="width: 100px;"></div>
                                <input type="file" class="form-control" name="pic">
                                <input type="hidden" name="old_pic" value="<%= pic %>">
                            </div>
                            <div class="form-group">
                                <label>Name</label>
                                <input type="text" class="form-control" name="name" value="<%= name %>">
                            </div>
                            <div class="form-group">
                                <label>Price</label>
                                <input type="text" class="form-control" name="price" value="<%= price %>">
                            </div>
                            <div class="form-group">
                                <label>Description</label>
                                <input type="text" class="form-control" name="descr" placeholder="Optional" value="<%= descr %>">
                            </div>
                            <div class="form-btn">
                                <button type="submit" class="btn btn-primary">Update</button>
                                <button type="reset" class="btn btn-warning">Reset</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script src="//cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.js"></script>

</body>

</html>